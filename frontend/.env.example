# URL base da API FastAPI (sem trailing slash)
# Em dev, o Vite proxy já encaminha /cases e /static para esta URL,
# por isso podes deixar vazio e o proxy trata tudo.
# Em produção, define o URL completo da API.
VITE_API_BASE_URL=http://localhost:8000
